~~1. verify if caching for the onboarding step is required or not.~~ ✅ Explicit `cache_control: { type: "ephemeral" }` already in place in questioning.ts, consolidate.ts, modules.ts — correct implementation, no further work needed.
~~1.5 aggregate overall costs, tokens, and other statistics into some records.~~ ✅ Usage tracking middleware added to getModel() — logs `[llm-usage] <JSON>` per call (model, in/out tokens, cache hit %, cost estimate, duration). Aggregate with `npm run usage`. ~~2. create 'test cases' to show the ability of the model/system to intuit things that make it better than hardcoded heuristics~~ ✅ digest.test.ts: 13 tests covering recovery day, adherence streak (forgive/reset), pain stagnation flag (key scenario: compliance OK but pain plateau → LLM must not progress the plan), delta vs baseline, and preamble grounding. ~~3. deal with proper database migration practice soon~~ ✅ Created migration 20260627000001_push_subscriptions_and_memory_versions for PushSubscription+PatientMemoryVersion tables that were previously db-pushed; resolved as applied; `prisma migrate status` now shows "Database schema is up to date!". Going forward use `prisma migrate dev --name <name>` for schema changes. ~~4. better abstractions for printing/logging: different suits printing different things?~~ ✅ lib/logger.ts: createLogger(namespace) factory returns { info, warn, error } with [namespace] prefix. Adopted in graphLogging.ts ([graph]), notifications/push.ts ([push]), notifications/service.ts ([cron]). Existing [prisma] and [llm-usage] already follow the same convention. ~~5. Regular notifications, both for normal updates like "hey! have you logged your progress today?" but also when things go wrong e.g patient not doing their compliance properly, and the system notices low compliance with their plan or higher pain scales being recorded across a few days.~~ ✅ lib/notifications/{email,push,service}.ts: Resend email + Web Push (VAPID) + runNotificationCron() with daily-nudge / low-compliance / pain-stagnation checks. app/api/notifications/{cron,subscribe} endpoints (CRON_SECRET-gated), PushSubscriber + sw.js. E2E in e2e-notifications.mjs. ~~6. "Press one button to generate report" feature.~~ ✅ generatePatientReportAction in lib/actions.ts: builds digest + pain series + compliance per day + flags + PatientMemory in one call (no LLM). components/admin/ReportTab.tsx: "Generate Report" button → inline SVG pain line chart + compliance bar chart (green/amber/red) + recovery flags card + heuristic digest + AI memory narrative (semantic + episodic phases). Wired as new "report" tab in PatientDetailView.

~~7. More thorough e2e testing with policies to verify features.~~ ✅ Policy-driven harness: the simulated patient is a REAL Claude agent (scripts/harness/\_simulator.mjs via @anthropic-ai/claude-agent-sdk), conditioned on the policy + onboarding profile + a running markdown memory (.dev/policy-sessions/<ts>.md). \_plan.mjs extracts the day's goal-bearing tasks and applies the agent's per-task decisions; succinct task-wise logging (e.g. "exercise:Ankle Pumps 6/20repetitions · … · pain 6/10"). Runs via `npm run harness:trajectory -- policy="..." [days= complianceThreshold= name= model=]`. Default sim model `haiku` (override SIM_MODEL). See scripts/harness/POLICY_HARNESS_PLAN.md. [Original spec text below.]

7. More thorough e2e testing with policies to verify features. The idea here is that if we want to verify a feature or the system works as intended: e.g if alert notifications fire as intended, or if we want to verify that the trajectory of the plan recommendations are correct, we should define policies to test with.

-   E.g begin test with a "reluctant policy". for whatever plan is returned that day, only fulfill by a certain percent. Have pain score be constant / not improving. Expected behaviour is for app notifications for user wellness to be raised due to low compliance with plan + not improving pain score. Policy here is what a subagent will condition itself to respond with, yes a subagent will be creating a harness for yourself to interact with the plan. This is to prevent context contamination to you, the main agent, who is in charge of the loop.

The idea for this part is to fully simulate app-patient interactions. Create a harness for you to respond to plans i.e a feedback loop between you and the system. Your conditioning should be the onboarding outputs, the policy, and some longer running memory in markdown files you increase somewhere for each test session, just in case you run out of context.

The harness you create should accept a policy argument, e.g npm run harness:name_of_test policy="LENGTHY_POLICY_STRING_HERE". The idea is that for a particular app interaction we want to test, we can pass in a detailed policy to condition the subagent on-the-fly.

Ensure detailed but succinct logging. For each app-patient interaction the subagent has, log the response to the plan task-wise e.g log leglifts 7/10 where 7 is the chosen amount done for the exercise.

Plan all features needed for the spec above, then implement.

~~7.5 Implement compliance and pain stagnation into the app backend / db.~~ ✅ DailyMetric table (one row per user/day: compliancePct, completedTasks, totalTasks, painScore; migration 20260628022538_add_daily_metric). lib/engagement/compliance.ts getDayCompliance() = completed/total goal-bearing tasks across ALL modules (symptoms goal:0 excluded). low_compliance flag added to flags.ts (mean daily compliance under threshold, default 50%, overridable). lib/metrics/service.ts upsertDailyMetric/getDailyMetrics/recomputeDailyMetricForModule; recompute hooked into updateModuleProgress. Dev route gained `metrics` op + compliance in `flags` op. 12 unit tests in compliance.test.ts.

pain scale is the same too, something daily to track. For these two, implement in the backend + db as desired, perhaps per-row per-day in a new table

~~8. Replace the e2e trajectory test with a 14-day policy simulation.~~ ✅ scripts/harness/e2e-trajectory.mjs rewritten: drives N (default 14) simulated days through the feature-7 harness, persists DailyMetric each day, then evaluates flags (pain_stagnation, low_compliance with overridable complianceThreshold) and triggers the user-scoped cron. Asserts FLAG ⟺ NOTIFICATION consistency with NO prior on which flags trip — it reports what the policy produced and checks the matching email/push was attempted. Verified: reluctant-policy 14-day run fired BOTH flags + sent both notifications (23/24 green; the 1 miss was a transient app-side state-gen LLM error on one day, which the harness tolerated and continued past). [Original spec text below.]

8. To test the above large feature 7 harness implementation, replace the existing e2e trajectory test with this. Simulate a 14 day interaction between you and the app using the harness you built in feature 7. This test should follow the syntax of npm run harness:trajectory policy="LENGTHY_POLICY_STRING_HERE". This test should have the following triggerable flags: pain stagnation, compliance (below some default threshold that may be overridden if passed into the command). a corresponding email/push notification should be sent too and verified that way. Do not ahve any priors about whether these flags will be tripped: the idea is to dynamically test based on policy strings passed into the command

~~9. Increase the context for state generation.~~ ✅ (via 9.1 audit + 9.2 impl) Premise corrected: plan-gen already gets compacted memory + full-history digest + raw window (not just yesterday). Added the missing mid-tier: rolling 7d/14d/30d trend aggregation from DailyMetric, injected into the digest; removed dead context. See docs/MEMORY_AUDIT.md.

9. Increase the context for state generation. Current we only send in the previous day's plan and progress, is this correct? however, for more intelligent plan generation, the next day's state plan should be generated with some context of what has happened so far in the past. the idea was to aggregate memories at various hierarchies e.g across the current sliding window, across the month or something like that? and selectively use them as additional context. memory is them read, maintained and written to accordingly.

~~9.1 first verify the implementation of memory in this app.~~ ✅ docs/MEMORY_AUDIT.md: full audit. Key findings — plan-gen already gets 3 context layers (compacted memory + full-history heuristic digest + raw window), NOT just yesterday; the "yesterday-only" bit is just the previous module JSON. Timespan abstractions: recovery phase (≈weeks, episodic narrative), watermark split (consolidated vs raw), contextWindowDays. Inefficiencies proven: `state.transcripts` computed but never consumed (dead, grows with history); `state.stateHistory` loaded but unused (N-day window = dead DB round-trips); `includeTrajectory`/`maxContextTokens`/smartFiltering config is vestigial (referenced nowhere effective); threads fetched twice; raw window unbounded until 4k-char threshold. Gaps for item 9: no mid-tier (weekly/monthly) rollup, no structured plan→outcome memory, phase-skip gap. Proposed 9.2: (A) remove dead context, (B) multi-window rolling trends from DailyMetric, (C) plan→outcome trace, (D) bound raw window.

9.1 first verify the implementation of memory in this app. for our intended agentic use case, what is currently in place? where is it inefficient or efficient, and where can it be improved? what abstractions currently exist over timespans?

~~9.2 implement improvements you identified in 9.1.~~ ✅ (A) Removed dead context: deleted state.transcripts + state.stateHistory annotation fields; load_context no longer builds the unused transcript string or runs the N-day loop / smartFiltering / filterSignificantStates / computeStateChangeScore / loadAllStateHistory (all proven dead in 9.1). previousState is now the most-recent prior state (robust to logging gaps). (B) lib/memory/rolling.ts buildRollingTrends() aggregates persisted DailyMetric over 7d / prior-7d (week-over-week ▲▼→) / 30d windows into a compact block appended to the heuristic digest — the missing mid-tier timespan ("across the sliding window / month"). 7 unit tests. Context-size logging added (item-10 groundwork). Verified: days=2 trajectory 12/12 green. C (plan→outcome trace) + D (bound raw window) documented as deferred follow-ups. See docs/MEMORY_AUDIT.md (§9.2).

9.2 implement improvements you identified in 9.1 in the app backend and document these changes thoroughly.

~~10. Add additional logs for context sizes / memory content / provenance + compression heuristics, then observe.~~ ✅ generation-context.ts assembleGenerationContext() = single source of truth, computes a MemoryObservability snapshot (layer sizes, memory content [semantic chars + per-phase episodic], provenance [raw-window msgs, doctor note, watermark, profileInSemantic], + compaction ratio). Structured [memory] + [memory-consolidation] logs; dev `context` op exposes it over HTTP; `npm run memory` report; e2e prints + asserts it. Looped on observation: fixed the compaction metric (digest was wrongly in denominator → now fullTranscript / (memory+rawWindow)), and found+fixed that the harness patient had no PatientMemory (now seeded like production onboarding). days=3 run: memory 1335c, raw 0c, compaction 1.06×, conversational context bounded. See docs/MEMORY_AUDIT.md §10.

10. Add additional logs for size of contexts, what is the actual content of the memories, what goes into it (what externals? where did the memories come from, what semantics are associated with each memory instance? anything else like profile is being included?), and some heuristics e.g compression of context per new memory created, or other things deemed as valuable in the industry. then rerun the e2e test and observe the logs, looping until you deem satisfactory amount of compression and effectiveness.

~~10.1 automation to test against various policies (reluctant / standard / worsening).~~ ✅ scripts/harness/policy-suite.mjs (`npm run harness:policies` [days= only= complianceThreshold=]) runs the trajectory across 3 contrasting Claude-agent policies and prints a comparison; trajectory emits a [[RESULT]] line for parsing. Running it surfaced that BINARY compliance made the diligent "standard" patient (~80%) look identical to the reluctant one (~4%) — so compliance is now GRADED (mean of min(value/goal,1)), flowing consistently through DailyMetric / low_compliance flag / rolling trends / report; strict completedTasks count retained. Contrast now: reluctant→low_compliance, standard→none, worsening→pain_stagnation. See docs/MEMORY_AUDIT.md §10.1.

10.1 to more thoroughly test the above, create automation to test against various policies, including the existing reluctant one, but also a "standard" case where the patient largely (but not exactly) follows the plan and their condition improves over time. And also another case where the plan is followed but condition stagnates/worsens.

11. Enhance notifications by contextualizing their generation, not just including the same message for "daily reminder" or "red alert when pain stagnation / non compliance". Generating against the previous day's memory to give more insightful notifications is preferrable.
